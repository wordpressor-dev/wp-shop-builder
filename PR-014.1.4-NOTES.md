# PR-014.1.4 — Admin Provider Lifecycle Test Fix

## Fix

Updated `AdminServiceProviderTest` to match the two-phase provider lifecycle:

1. `register()` configures container services only.
2. `boot()` registers the `admin_menu` WordPress hook.

The test now explicitly verifies that no hook exists after `register()`, calls `boot()`, and then verifies the hook registration.

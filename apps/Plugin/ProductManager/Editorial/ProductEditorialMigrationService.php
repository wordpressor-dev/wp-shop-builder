<?php
 declare(strict_types=1); namespace WPShop\App\Plugin\ProductManager\Editorial; use Closure; use RuntimeException; use Throwable; use WPShop\App\Plugin\ProductManager\CatalogProductType; use WPShop\App\Plugin\ProductManager\Envato\Contracts\EnvatoClientInterface; final class ProductEditorialMigrationService { private const BACKUP_META = '_wp_shop_editorial_backup_v28'; private const STANDARD_META = '_wp_shop_editorial_standard'; private const MIGRATED_AT_META = '_wp_shop_editorial_migrated_at'; public function __construct( private readonly Closure $call, private readonly ?EnvatoClientInterface $envato = null, private readonly ProductEditorialDraftBuilder $builder = new ProductEditorialDraftBuilder(), private readonly EnvatoOfficialFactsExtractor $officialExtractor = new EnvatoOfficialFactsExtractor() ) { } /** @return array{productId:int,title:string,baseTitle:string,status:string,productType:string,developer:string,sourceUpdateDate:string,ruStatus:string,enStatus:string,metaStatus:string,backupAvailable:bool,officialStatus:string,officialFacts:int,current:array{ruShort:string,ruLong:string,ruMeta:string,enShort:string,enLong:string,enMeta:string},generated:array{ruShort:string,ruLong:string,ruMeta:string,enShort:string,enLong:string,enMeta:string}} */ public function preview(int $productId): array { $post = ($this->call)('get_post', $productId); $row = is_object($post) ? get_object_vars($post) : []; if (($row['post_type'] ?? '') !== 'product') { throw new RuntimeException('Product not found: ' . $productId); } $title = trim((string) ($row['post_title'] ?? '')); $baseTitle = $this->baseTitle($productId, $title); $productType = $this->productType($productId, $baseTitle); $developer = $this->meta($productId, 'attr_developer_value'); $sourceUpdateDate = $this->meta( $productId, '_wp_shop_source_update_date' ); $current = [ 'ruShort' => (string) ($row['post_excerpt'] ?? ''), 'ruLong' => (string) ($row['post_content'] ?? ''), 'ruMeta' => $this->sureRankMeta($productId), 'enShort' => $this->meta($productId, '_wp_shop_en_short_description'), 'enLong' => $this->meta($productId, '_wp_shop_en_long_description'), 'enMeta' => $this->meta($productId, '_wp_shop_en_meta_description'), ]; $backup = ($this->call)( 'get_post_meta', $productId, self::BACKUP_META, true ); $signals = $this->signals($productId, $baseTitle); $official = $this->officialFacts($productId); $signals = str_starts_with($official['status'], 'READY') ? array_values(array_unique(array_merge( $this->semanticSignals($signals), $official['signals'] ))) : array_values(array_unique(array_merge( $signals, $official['signals'] ))); $generic = $this->builder->build( $baseTitle, $developer, $productType, $signals, $sourceUpdateDate ); $generated = $generic; $legacy = null; if (is_array($backup) && $backup !== []) { $legacy = $backup; } elseif (! $this->sameList( array_values($current), array_values($generic) )) { $legacy = $current; } if (is_array($legacy)) { $generated = $this->builder->build( $baseTitle, $developer, $productType, $signals, $sourceUpdateDate, $this->enrichLegacy($legacy, $official, $generic) ); } $ruStatus = $this->status( [$current['ruShort'], $current['ruLong']], [$generated['ruShort'], $generated['ruLong']] ); $enStatus = $this->status( [$current['enShort'], $current['enLong'], $current['enMeta']], [$generated['enShort'], $generated['enLong'], $generated['enMeta']] ); $metaStatus = $this->singleStatus( $current['ruMeta'], $generated['ruMeta'] ); return [ 'productId' => $productId, 'title' => $title, 'baseTitle' => $baseTitle, 'status' => $ruStatus === 'CURRENT' && $enStatus === 'CURRENT' && $metaStatus === 'CURRENT' ? 'CURRENT' : 'MIGRATE', 'productType' => $productType, 'developer' => $developer, 'sourceUpdateDate' => $sourceUpdateDate, 'ruStatus' => $ruStatus, 'enStatus' => $enStatus, 'metaStatus' => $metaStatus, 'backupAvailable' => is_array($backup) && $backup !== [], 'officialStatus' => $official['status'], 'officialFacts' => count($official['ruFacts']), 'current' => $current, 'generated' => $generated, ]; } /** @return list<string> */ public function apply(int $productId): array { $preview = $this->preview($productId); if ($preview['status'] === 'CURRENT') { return [ 'EDITORIAL MIGRATION = NO CHANGE', 'PRODUCT ID = ' . $productId, 'EDITORIAL STANDARD = CURRENT', ]; } $backup = ($this->call)( 'get_post_meta', $productId, self::BACKUP_META, true ); $backupLog = 'EDITORIAL BACKUP = REUSED'; if (! is_array($backup) || $backup === []) { $backup = array_merge( ['created_at' => $this->now()], $preview['current'] ); ($this->call)( 'update_post_meta', $productId, self::BACKUP_META, $backup ); $backupLog = 'EDITORIAL BACKUP = CREATED'; } $this->writeContent($productId, $preview['generated']); ($this->call)('update_post_meta', $productId, self::STANDARD_META, 'v28'); ($this->call)( 'update_post_meta', $productId, self::MIGRATED_AT_META, $this->now() ); return [ 'EDITORIAL MIGRATION = READY', 'PRODUCT ID = ' . $productId, $backupLog, 'OFFICIAL ENVATO ENRICHMENT = ' . $preview['officialStatus'], 'OFFICIAL FACTS USED = ' . $preview['officialFacts'], 'RU SHORT / LONG = UPDATED', 'SURERANK META DESCRIPTION = UPDATED', 'EN SHORT / LONG / META = PREPARED', 'TRANSLATEPRESS = NOT TOUCHED', 'EDITORIAL STANDARD = v28', ]; } /** @return list<string> */ public function restore(int $productId): array { $backup = ($this->call)( 'get_post_meta', $productId, self::BACKUP_META, true ); if (! is_array($backup) || $backup === []) { throw new RuntimeException( 'Editorial backup not found for product ' . $productId ); } $this->writeContent($productId, [ 'ruShort' => (string) ($backup['ruShort'] ?? ''), 'ruLong' => (string) ($backup['ruLong'] ?? ''), 'ruMeta' => (string) ($backup['ruMeta'] ?? ''), 'enShort' => (string) ($backup['enShort'] ?? ''), 'enLong' => (string) ($backup['enLong'] ?? ''), 'enMeta' => (string) ($backup['enMeta'] ?? ''), ]); ($this->call)('delete_post_meta', $productId, self::STANDARD_META); ($this->call)('delete_post_meta', $productId, self::MIGRATED_AT_META); return [ 'EDITORIAL RESTORE = READY', 'PRODUCT ID = ' . $productId, 'RU / EN / META = RESTORED FROM BACKUP', 'BACKUP = PRESERVED', ]; } /** @param array<string,mixed> $legacy @param array{status:string,signals:list<string>,ruFacts:list<string>,enFacts:list<string>} $official @param array{ruShort:string,ruLong:string,ruMeta:string,enShort:string,enLong:string,enMeta:string} $generic @return array<string,mixed> */ private function enrichLegacy( array $legacy, array $official, array $generic ): array { $ruShort = $this->plainEditorialText( (string) ($legacy['ruShort'] ?? '') ); $ruLong = $this->plainEditorialText( (string) ($legacy['ruLong'] ?? '') ); if ($official['ruFacts'] !== []) { if ($this->sameEditorialText($ruShort, $ruLong)) { $tail = $this->detailTail($ruLong); if ($tail !== '') { $legacy['ruLong'] = $tail; } } $legacy['ruLong'] = $this->appendFacts( (string) ($legacy['ruLong'] ?? ''), $official['ruFacts'] ); } $enShort = $this->plainEditorialText( (string) ($legacy['enShort'] ?? '') ); $enLong = $this->plainEditorialText( (string) ($legacy['enLong'] ?? '') ); if ($official['enFacts'] !== []) { if ($enShort === '' && $enLong === '') { $legacy['enShort'] = $generic['enShort']; $legacy['enLong'] = ''; } elseif ($this->sameEditorialText($enShort, $enLong)) { $tail = $this->detailTail($enLong); if ($tail !== '') { $legacy['enLong'] = $tail; } } $legacy['enLong'] = $this->appendFacts( (string) ($legacy['enLong'] ?? ''), $official['enFacts'] ); } return $legacy; } /** @param list<string> $facts */ private function appendFacts(string $content, array $facts): string { $content = trim($content); $existing = mb_strtolower($this->plainEditorialText($content), 'UTF-8'); $newFacts = []; foreach ($facts as $fact) { $fact = trim($fact); if ($fact === '') { continue; } if (mb_strpos($existing, mb_strtolower($fact, 'UTF-8')) !== false) { continue; } $newFacts[] = $fact; } if ($newFacts === []) { return $content; } $line = implode(', ', array_values(array_unique($newFacts))) . '.'; return $content === '' ? $line : $content . ' ' . $line; } private function sameEditorialText(string $left, string $right): bool { if ($left === '' || $right === '') { return false; } return $this->normalize($left) === $this->normalize($right); } private function plainEditorialText(string $content): string { $content = html_entity_decode( strip_tags($content), ENT_QUOTES | ENT_HTML5, 'UTF-8' ); return trim((string) preg_replace('/\s+/u', ' ', $content)); } private function detailTail(string $content): string { $parts = preg_split( '/(?<=[.!?])\s+/u', trim($content), 2 ); if (! is_array($parts) || count($parts) < 2) { return ''; } return trim((string) $parts[1]); } /** @param list<string> $signals @return list<string> */ private function semanticSignals(array $signals): array { $technical = [ 'elementor', 'elementor pro', 'woocommerce', 'wpml', 'rtl', 'loco translate', 'translation ready', 'learnpress', 'learndash', 'lifterlms', 'sensei', 'tutor', 'tutor lms', 'bbpress', 'buddypress', 'gutenberg', 'contact form 7', ]; $result = []; foreach ($signals as $signal) { $normalized = mb_strtolower(trim($signal), 'UTF-8'); if ($normalized === '' || in_array($normalized, $technical, true)) { continue; } $result[] = trim($signal); } return array_values(array_unique($result)); } /** @return array{status:string,signals:list<string>,ruFacts:list<string>,enFacts:list<string>} */ private function officialFacts(int $productId): array { $empty = [ 'status' => 'NOT REQUESTED', 'signals' => [], 'ruFacts' => [], 'enFacts' => [], ]; if ($this->envato === null || ! $this->officialRequested($productId)) { return $empty; } $salesPage = $this->meta($productId, 'sales_page'); $token = $this->envatoToken(); if ($salesPage === '' || $token === '') { $empty['status'] = 'NOT AVAILABLE'; return $empty; } try { $facts = $this->officialExtractor->extract( $this->envato->fetch($salesPage, $token)->source ); $facts['status'] = $facts['ruFacts'] === [] ? 'READY / NO EXTRA FACTS' : 'READY'; return $facts; } catch (Throwable) { $empty['status'] = 'FAILED / LEGACY FALLBACK'; return $empty; } } private function officialRequested(int $productId): bool { if ( (int) ($_REQUEST['preview_id'] ?? 0) === $productId || (int) ($_POST['editorial_apply_id'] ?? 0) === $productId ) { return true; } $selected = $_POST['editorial_selected'] ?? []; if (! is_array($selected)) { return false; } foreach ($selected as $value) { if ((int) $value === $productId) { return true; } } return false; } private function envatoToken(): string { if (defined('WP_SHOP_ENVATO_TOKEN')) { $configured = constant('WP_SHOP_ENVATO_TOKEN'); if (is_string($configured) && trim($configured) !== '') { return trim($configured); } } return trim((string) ($this->call)( 'get_option', 'wp_shop_envato_personal_token', '' )); } /** @param array{ruShort:string,ruLong:string,ruMeta:string,enShort:string,enLong:string,enMeta:string} $content */ private function writeContent(int $productId, array $content): void { $updated = ($this->call)( 'wp_update_post', [ 'ID' => $productId, 'post_excerpt' => (string) ($this->call)( 'wp_kses_post', $content['ruShort'] ), 'post_content' => (string) ($this->call)( 'wp_kses_post', $content['ruLong'] ), ], true ); if (is_int($updated) && $updated <= 0) { throw new RuntimeException('Product content update failed.'); } if (is_object($updated) && method_exists($updated, 'get_error_message')) { throw new RuntimeException( 'Product content update failed: ' . (string) $updated->get_error_message() ); } $settings = ($this->call)( 'get_post_meta', $productId, 'surerank_settings_general', true ); $settings = is_array($settings) ? $settings : []; $settings['page_description'] = (string) ($this->call)( 'sanitize_textarea_field', $content['ruMeta'] ); ($this->call)( 'update_post_meta', $productId, 'surerank_settings_general', $settings ); foreach ([ '_wp_shop_en_short_description' => $content['enShort'], '_wp_shop_en_long_description' => $content['enLong'], ] as $key => $value) { ($this->call)( 'update_post_meta', $productId, $key, (string) ($this->call)('wp_kses_post', $value) ); } ($this->call)( 'update_post_meta', $productId, '_wp_shop_en_meta_description', (string) ($this->call)( 'sanitize_textarea_field', $content['enMeta'] ) ); } private function baseTitle(int $productId, string $title): string { $version = trim($this->meta($productId, 'attr_version_value')); if ($version === '' || $version === '—') { return $title; } $suffix = ' ' . $version; return str_ends_with($title, $suffix) ? trim(substr($title, 0, -strlen($suffix))) : $title; } private function productType(int $productId, string $baseTitle): string { $stored = trim($this->meta($productId, '_wp_shop_product_type')); if (in_array($stored, [ CatalogProductType::THEME, CatalogProductType::PLUGIN, CatalogProductType::TEMPLATE_KIT, ], true)) { return $stored; } $category = strtolower(trim($this->meta( $productId, 'attr_category_value' ))); if (in_array($category, ['шаблоны', 'templates'], true)) { return CatalogProductType::TEMPLATE_KIT; } if (in_array($category, ['плагины', 'plugins'], true)) { return CatalogProductType::PLUGIN; } if (in_array($category, ['темы', 'themes'], true)) { return CatalogProductType::THEME; } return CatalogProductType::infer( $baseTitle, $this->meta($productId, 'sales_page') ); } /** @return list<string> */ private function signals(int $productId, string $baseTitle): array { $signals = []; foreach (['product_tag', 'pa_tags'] as $taxonomy) { $terms = ($this->call)( 'wp_get_post_terms', $productId, $taxonomy, ['fields' => 'names'] ); if (! is_array($terms)) { continue; } foreach ($terms as $term) { if (is_scalar($term) && trim((string) $term) !== '') { $signals[] = trim((string) $term); } } } $titleParts = preg_split('/[^a-z0-9-]+/i', $baseTitle) ?: []; foreach ($titleParts as $part) { if (strlen(trim($part)) >= 4) { $signals[] = trim($part); } } if (str_contains(strtolower($baseTitle), 'real estate')) { $signals[] = 'real estate'; } return array_values(array_unique($signals)); } private function sureRankMeta(int $productId): string { $settings = ($this->call)( 'get_post_meta', $productId, 'surerank_settings_general', true ); return is_array($settings) ? trim((string) ($settings['page_description'] ?? '')) : ''; } private function meta(int $productId, string $key): string { $value = ($this->call)('get_post_meta', $productId, $key, true); return is_scalar($value) ? trim((string) $value) : ''; } private function singleStatus(string $current, string $generated): string { if (trim($current) === '') { return 'MISSING'; } return $this->normalize($current) === $this->normalize($generated) ? 'CURRENT' : 'OLD'; } /** @param list<string> $current @param list<string> $generated */ private function status(array $current, array $generated): string { if ($this->allEmpty($current)) { return 'MISSING'; } return $this->sameList($current, $generated) ? 'CURRENT' : 'OLD'; } /** @param list<string> $values */ private function allEmpty(array $values): bool { foreach ($values as $value) { if (trim($value) !== '') { return false; } } return true; } /** @param list<string> $left @param list<string> $right */ private function sameList(array $left, array $right): bool { if (count($left) !== count($right)) { return false; } foreach ($left as $index => $value) { if ($this->normalize($value) !== $this->normalize($right[$index])) { return false; } } return true; } private function normalize(string $value): string { return trim((string) preg_replace('/\s+/u', ' ', $value)); } private function now(): string { return (string) ($this->call)('current_time', 'mysql'); } } 